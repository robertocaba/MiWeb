Ejercicios variables Bikram1-Crear variable tipo let sin valor:      let myVariable ;2-Crear dos variables de tipo booleano:	let booleano1 = true;	let booleano2 = false;3-Crear variable tipo const con valor 3.14:	const pi = 3.14;4- Crear variable tipo const con valor dos veces 3.14;	const tau = 2 * pi ;5-Crear variable booleano cuyo valor sea la comparaci—n de booleano1 y booleano2:	const booleano = boolenao1 == booleano2;6- Crear variable booleanoNot cuyo valor sea la comparaci—n de booleano no booleano2:	const booleanoNot = booleano != booleano2;7- Crear variable booleanoMix0 cuyo valor sea la comparaci—n de booleana (booleano1 o booleano2) y (booleano1 o (no booleano2):const booleanoMix0 = (booleano1 ||booleano2 && (booleano1 || (!booleano2));8-Crear Variable incrementarDesp con valor 2 y asigna su valor con postIncremento a resultadoDesp:	let incrementarDesp = 2;	let resultadoDesp = incrementarDesp++;Console.log (resultadoDesp);9- Crear Variable incrementarAntes con valor 2 y asigna su valor con preIncremento a resultadoAntes:	let incrementarAntes = 2;	let resultadoAntes = ++incrementarAntes;Console.log (resultadoAntes);10-Crear variable contarHast10_2 con valor 0 e incrementar su valor con un bucle for hasta que valga 10:	const contarHasta10_2 = 0;	let i = contarHasta10_2;	for (; i <  10; i++) {	console.log (contarHast10_2);      }	11-Crear las variables postI, postJ con valor 0 a continuaci—n crea un bucle que itere 11 veces. En cada iteraci—n debe sumar al valor postI el valor postJ:	let postI = 0;	let postJ = 0;	for (let i =0; i < = 10; i++) {	postJ += postI++;	console.log(postI, postJ);	}12- Crear la variable sumaPares con valor 0 a continuaci—n crea un bucle que itere 10 veces. si es par se deber‡ sumar a sumaPares el nœmero de la iteraci—n actual:	const sumaPares = 0;	for (let i=0; i < 10; i++) {	if (i % 2 =0) { sumaPares += i       console.log (ÒparesÓ, i);      }      else { 	console.log ( ÒimparesÓ, i);	}13- Crear la variable tipo let de nombre variableValorNumerico declarado con un valor numŽrico cualquiera.	let  variableValorNumerico = 3;14- Crear la variable tipo const de nombre miNombre declarada con tu nombre:	const miNombre = ÒRobertoÓ;	console.log (miNombre);15- Crear la variable tipo const de nombre miNumeroFav declarada con valor:	const miNumeroFav = 5;	console.log (miNumeroFav);16- Crear la variable booleano0 cuyo valor sea la comparaci—n booleana de booleano1 y booleano2:	const booleano0 = booleano1 == booleano2;	console.log (booleano0);17- Crear la variable booleanoMix1 cuyo valor sea la comparaci—n booleana de (booleano1 y (tau/2 sea igual a pi)) o (variableValorNumerico mayor o igual que miNumeroFav):const booleanoMix1 = booleano1 && (tau/2 == pi) || (variableValorNumerico >= miNumeroFav);	console.log (booleanoMix1);18- Crear la variable seisNoEsNueve cuyo valor sea la comparaci—n booleana 6 no es igual a 9;	const seisNoEsNueve = 6!= 9;	console.log (seisNoEsNueve);19- Crear la variable booleanoMix2 cuyo valor sea la comparaci—n booleana de variableValorNumerico positivo (0 no incluido) o menor que miNumeroFav por tau:	const booleanoMix2 = variableValorNumerico < (miNumeroFav * tau) ;	console.log (booleanoMix2);20- Crear la variable valorSuma cuyo valor sea la suma de miNumeroFav y variableValorNumerico:	let valorSuma = miNumeroFav + variableValorNumerico;	console.log (valorSuma);21- Crear la variable valorResta cuyo valor sea la resta de miNumeroFav y variableValorNumerico:	let valorResta = miNumeroFav - variableValorNumerico;	console.log (valorResta);22- Crear la variable valorMultiplicaci—n cuyo valor sea la Multiplicaci—n de miNumeroFav y variableValorNumerico:	let valorMultiplicacon = miNumeroFav * variableValorNumerico;	console.log (valorMultiplicacon);23- Crear la variable valorDivisi—n cuyo valor sea la Divisi—n de miNumeroFav entre 3:	let valorDivision = miNumeroFav / 3;	console.log (valorDivision);24- Crear variable contarHast10 con valor 0 e incrementar su valor con un bucle while hasta que valga 10:	const contarHasta10 = 0;		while (contarHasta10 < 10) {	contarHasta10 ++;	console.log (contarHast10);      }25- Crear las variables preI, preJ con valor 0 a continuaci—n crea un bucle que itere 11 veces. En cada iteraci—n debe sumar al valor preI el valor preJ:	let preI = 0;	let preJ = 0;	for (let preI=0; preI < = 10; preI ++) {	preJ += ++preI;	console.log(preI, preJ);	}26- Crear la variable sumaImpares con valor 0 a continuaci—n crea un bucle que itere 10 veces. si es impar se deber‡ sumar a sumaImpares el nœmero de la iteraci—n actual:	const sumaImpares = 0;	for (let i=0; i < 10; i++) {	if (i % 2 !=0) { sumaImpares += i       console.log (ÒimparesÓ, i);      }      else { 	console.log ( ÒparesÓ, i);	}	